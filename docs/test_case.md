# DataFinderAgentOS 测试用例学习记录

## 文档定位
- 本文件记录当前项目在学习阶段已观察到的测试方式、已执行结果以及建议补充的测试点。
- 当前内容以“理解项目现状、为后续修复和开发做准备”为目标，不等同于完整测试计划。

## 已发现的测试脚本
- `test/test_user_models.py`

## 已执行命令
```powershell
.\venv\Scripts\python.exe .\test\test_user_models.py
```

## 已执行结果
- 结果：失败
- 失败位置：`app/models/user.py`
- 失败原因：`UserRepository.create_user()` 调用 `__hash_password()` 时触发名称改写，抛出 `NameError`

## 当前可确认的测试主题

### 1. 数据库初始化
- 用例目标：执行 `init_db()` 后自动创建 `users` 表。
- 预期结果：数据库文件存在，`users` 表可查询。
- 当前状态：代码具备该能力，但未单独拆成断言式测试。

### 2. 用户创建
- 用例目标：使用新用户名创建用户。
- 预期结果：首次创建成功，返回 `True`。
- 当前状态：受哈希函数调用错误影响，当前失败。

### 3. 重复用户创建
- 用例目标：同一用户名重复创建两次。
- 预期结果：第一次成功，第二次因唯一约束失败并返回 `False`。
- 当前状态：理论逻辑已写，需在修复用户创建后复测。

### 4. 密码校验
- 用例目标：校验正确密码、错误密码和不存在用户三种情况。
- 预期结果：正确密码返回 `True`，其余返回 `False`。
- 当前状态：接口逻辑已实现，但需在用户创建可用后才能稳定验证。

### 5. 登录页渲染
- 用例目标：访问 `/` 返回登录页。
- 预期结果：页面成功渲染，显示用户名和密码输入框。
- 当前状态：路由存在，页面模板存在，尚未进行接口级验证。

### 6. 登录提交
- 用例目标：提交用户名和密码到登录接口。
- 预期结果：参数为空时返回错误，认证成功时写入 Cookie，认证失败时返回错误提示。
- 当前状态：存在流程设计，但前端表单缺少 `method="post"`，控制器还存在 `verify_user` 调用拼写问题，当前主链路不可用。

### 7. 退出登录
- 用例目标：已登录用户提交 `/logout`。
- 预期结果：清理 `username` Cookie 并跳回登录页。
- 当前状态：控制器和页面表单已存在，建议后续联调验证。

### 8. 首页访问保护
- 用例目标：未登录用户访问 `/index`。
- 预期结果：应跳转到登录页。
- 当前状态：当前代码未加保护，属于待补充测试点。

### 9. 前端资源接入
- 用例目标：验证页面能够正确加载 `app/static/dist/` 中的本地化 Layui 与 Bootstrap 资源。
- 预期结果：静态资源路径有效，页面样式和脚本可正常返回。
- 当前状态：资源已存在，但模板尚未正式统一接入。

### 10. Layui 优先界面规范
- 用例目标：验证前台和后台页面优先使用 Layui 组件构建界面。
- 预期结果：通用表单、导航、表格、按钮等核心界面优先采用 Layui 风格与组件。
- 当前状态：属于后续页面重构与开发时的检查项。

### 11. WebSocket 通信
- 用例目标：验证大模型对话场景下的双向实时消息通道是否可用。
- 预期结果：客户端与服务端可建立连接，消息可双向收发，断线与异常可被处理。
- 当前状态：技术栈已补充，仓库中尚未落地实现。

### 12. SSE 事件流
- 用例目标：验证服务端推送消息流在对话输出场景下是否稳定。
- 预期结果：客户端可持续接收增量事件，流结束标记明确，中断时具备错误处理。
- 当前状态：技术栈已补充，仓库中尚未落地实现。

### 13. OpenAI 对话接口
- 用例目标：验证与 OpenAI 相关接口的请求构造、鉴权、流式返回和异常处理。
- 预期结果：可完成一次完整对话请求，并在失败时返回清晰错误信息。
- 当前状态：技术栈已补充，仓库中尚未落地实现。

### 14. 用户侧模块覆盖
- 用例目标：验证用户侧页面是否覆盖登录、注册、对话、数字员工、报表、历史记录、报告导出等基础模块。
- 预期结果：用户侧入口清晰，页面归属明确，同级模板仅承载前台功能。
- 当前状态：目前仅有登录页和首页骨架，其他模块尚未实现。

### 15. 管理侧模块覆盖
- 用例目标：验证后台是否覆盖用户管理、功能管理、菜单管理、角色管理、瞭望管理、数据管理、采集管理、数字员工、模型引擎、数智大屏、舆情大屏等模块。
- 预期结果：`app/templates/admin/` 下页面仅承载后台管理功能，菜单与权限结构清晰。
- 当前状态：`admin/` 目录已存在，但仍为模板占位状态。

### 16. 模板目录归属
- 用例目标：验证后台页面全部放置于 `app/templates/admin/`，用户侧页面放置于 `app/templates/` 同级目录。
- 预期结果：前后台页面目录边界清晰，不出现模板混放。
- 当前状态：目录约定已明确，需在后续开发中持续校验。

### 17. 响应式设计
- 用例目标：验证系统页面在常见桌面端、平板端和移动端宽度下均能正常显示和操作。
- 预期结果：布局不破坏，导航可用，表单、图表、对话区和数据卡片具备良好适配能力。
- 当前状态：尚未开始系统化页面实现，属于后续重点测试项。

### 18. 自适应模式
- 用例目标：验证系统在移动端与 PC 端下根据设备差异调整交互与信息密度。
- 预期结果：移动端优先保证核心操作闭环，PC 端优先保证信息展示深度和多区域协同。
- 当前状态：尚未落地，需在模板基座设计完成后联调验证。

### 19. 沉浸式风格一致性
- 用例目标：验证前台和后台是否保持统一的视觉语言、主题风格和组件表达。
- 预期结果：页面主色、层次、留白、动效和数据展示风格统一，不出现明显割裂。
- 当前状态：当前页面仍为骨架状态，后续需结合 UI 实现验收。

### 20. 配置目录治理
- 用例目标：验证系统运行配置、安全配置、模型配置和环境配置是否统一收敛到 `config/` 目录。
- 预期结果：配置文件位置统一，业务代码中不散落敏感配置或关键运行参数。
- 当前状态：当前仍存在配置直接写在 `run.py` 的情况，后续需迁移。

### 21. OWASP Top 10 安全策略
- 用例目标：验证系统对 OWASP Top 10 主要漏洞是否具备明确策略和对应机制。
- 预期结果：对认证、授权、注入、XSS、CSRF、敏感信息暴露、依赖安全、日志与错误处理等均有防护与验证方案。
- 当前状态：目前仅具备部分基础能力，尚未形成完整安全测试清单。

### 22. 前后台路由边界
- 用例目标：验证后台页面统一位于 `/admin/` 路由空间，前台页面统一位于根路径路由空间。
- 预期结果：后台入口为 `http://xxx/admin/`，前台入口为 `http://xxx/`，两侧路由不混用。
- 当前状态：当前代码尚未完成该路由体系的正式落地。

### 23. 后台路由访问控制
- 用例目标：验证后台 `/admin/` 下各功能页面具备独立访问控制和权限校验能力。
- 预期结果：未授权用户不能通过猜测 URL 直接访问后台页面。
- 当前状态：路由规范已明确，权限体系尚未实现。

### 24. 前台路由一致性
- 用例目标：验证用户登录、注册、对话、历史记录、报表等页面在前台根路径体系中组织清晰。
- 预期结果：前台路由语义统一，页面跳转与导航结构一致。
- 当前状态：当前仅有少量骨架路由，尚未形成完整前台路由表。

---

## 任务3 测试结果（瞭望管理 + 瞭源管理 + 百度新闻采集）

### 测试日期
2026-07-14

### 测试环境
- 服务运行地址：http://localhost:10010
- 测试用户：admin / 123456
- 测试方式：API 接口测试（含 XSRF 认证）

### 测试用例 1：瞭源列表API
- 测试目标：`GET /api/sources/active` 返回所有启用的瞭源
- 测试结果：**通过** ✓
- 返回数据：成功返回 `success: true`，data 中包含 1 条百度新闻瞭源
- 验证内容：百度新闻瞭源的 URL 模板、请求头、关键词参数（word）、分页参数（pn）、分页步长（10）均正确

### 测试用例 2：数据采集API
- 测试目标：`POST /api/watch/collect` 使用百度新闻瞭源采集关键词"四川"的新闻
- 测试结果：**通过** ✓
- 返回数据：`{"success": true, "message": "采集完成，共获取 10 条数据", "count": 10}`
- 验证内容：成功从百度新闻搜索结果中解析出 10 条新闻，包含标题、URL、来源名称

### 测试用例 3：采集数据查询API
- 测试目标：`GET /api/watch/data` 查询已采集的数据
- 测试结果：**通过** ✓
- 返回数据：`total: 10`, 共返回 10 条数据
- 验证内容：
  - 数据格式为橱窗列表模式
  - 每条数据包含 title、url、summary、source_name、collected_at 字段
  - 示例标题："上千名小将角逐四川省青少年乒乓球公开赛"、"今夏首次!四川电网用电负荷创新高" 等
  - 示例URL：`https://baijiahao.baidu.com/s?id=...`，链接真实可访问

### 测试结论
**百度新闻数据采集功能可用** ✓
- 百度新闻规则已成功落库到 `watch_sources` 表
- 通过 RequestHeaders 模拟浏览器请求，成功获取百度新闻搜索结果
- HTML 解析逻辑能正确提取新闻标题、链接和摘要
- 采集数据可正确存储到 `watch_collected_data` 表并通过 API 查询
- 当前采集第一页（pn=0），每页最多提取 50 条有效新闻

---

## 任务3.1 测试结果（更名、参数可视化、子系统重构）

### 测试日期
2026-07-14

### 测试用例 1：功能结构验证（数据库）
- 测试目标：verify the new module organization in the database
- 测试结果：**通过** ✓
- 验证数据：
  - 瞭望中心（ID=3, top-level, sort_order=3, icon=layui-icon-engine）  ✓
  - ├ 瞭源管理（ID=8, parent_id=3, sort_order=1）                    ✓
  - └ 瞭望采集（ID=9, parent_id=3, sort_order=2, name=瞭望采集）     ✓
  - 管理系统下仅有4个子功能（用户/角色/功能/菜单管理）              ✓

### 测试用例 2：B区参数可视化
- 测试目标：验证瞭望采集页面B区展示参数详情
- 测试结果：**通过** ✓
- 验证内容：
  - 每个瞭源以卡片形式展示，包含 switch-header（名称+方法标签）+ source-params（参数详情面板）
  - 选中瞭源后展开参数面板，显示 URL模板、关键词参数（word）、分页参数（pn）、分页步长（10）、请求方式（GET）
  - 支持多选，选中状态 visual 指示清晰

### 测试用例 3：菜单导航验证
- 测试目标：验证左侧菜单导航结构符合"瞭望中心"子系统
- 测试结果：**通过** ✓
- 验证内容：Menu count = 9，所有功能都有对应的菜单条目

---

## 任务3.2 测试结果（原文链接修复 + B区分页参数）

### 测试日期
2026-07-14

### 修复内容
1. **原文链接触发机制**：
   - 整张卡片可点击（点击卡片任意位置打开原文）
   - 新增蓝色"查看原文"按钮，`z-index` 确保不被卡片覆盖层阻挡
   - 标题链接保持可单独点击（`event.stopPropagation()`）
   - 后端URL规范化：处理协议相对路径(`//...`)、根相对路径(`/...`)、无协议路径

2. **B区分页参数**：
   - 新增"采集页数"数字输入框（默认第1页）
   - 显示"每页步长"值（从选中瞭源的 `page_step` 自动读取同步）
   - 采集API接收 `page` 参数，计算 `(page-1) * page_step` 作为偏移量

### 测试用例 1：分页采集验证
- 测试目标：`POST /api/watch/collect` 传入 `page=2` 采集第二页
- 测试结果：**通过** ✓
- 返回数据：`{"success": true, "message": "采集完成，共获取 10 条数据", "count": 10}`

### 测试用例 2：URL规范化验证
- 测试目标：验证采集数据中所有URL为有效HTTP(S)链接
- 测试结果：**通过** ✓
- 采样数据：展示的5条新闻URL均为 `https://news.cctv.com/...` 等有效链接
- 累计采集数据：50条，均可正常打开原文

### 测试结论
**两个修复均已通过验证** ✓
- 原文链接在卡片上可通过3种方式触发：整卡点击 / 标题链接 / "查看原文"按钮
- B区支持手动输入采集页数，步长从瞭源配置自动同步

---

## 任务3.3 测试结果（步长可编辑同步 + 全选/全不选）

### 测试日期
2026-07-14

### 修改内容
1. **每页步长可编辑**：B区步长由只读文本改为 `<input>` 数字输入框 + "保存"按钮；修改后点击保存，通过 `/api/sources/update` 批量更新所有选中瞭源的 `page_step`，数据卡片和本地缓存同步刷新。
2. **全选/全不选**：B区标题栏右侧新增切换按钮，支持一键全选/取消所有瞭源；选中状态实时反映在按钮文字和样式上（蓝色高亮 = 全选状态）。

### 测试用例：步长修改同步
- 测试目标：修改B区步长 → 保存 → 验证瞭源管理数据库已更新
- 测试结果：**通过** ✓
- 验证数据：page_step 从 10 → 20，API 返回 `{"success": true}`，数据库查询确认已持久化

---

## 任务4.1 测试结果（模型引擎布局修复 + 图标更换）

### 测试日期
2026-07-14

### 修改内容
1. **布局修复**：模型引擎页面从自定义全屏沉浸式布局改为标准 Admin 上下+左右分区布局（`layui-layout-admin`），保留沉浸式深色风格在 `.layui-body` 工作区内。
2. **图标更换**：模型引擎图标从 `layui-icon-engine` 更换为 `layui-icon-component`，避免与瞭望中心图标重复。

### 测试用例 1：Admin 布局验证
- 测试目标：验证模型引擎页面使用标准 admin 布局结构
- 测试结果：**通过** ✓
- 验证内容：
  - 页面包含 `<div class="layui-layout layui-layout-admin">` 容器
  - `.layui-header`：包含 LOGO + 用户菜单（用户名、个人信息、修改密码、退出登录）
  - `.layui-side`：包含动态菜单树（与 base.html 一致）
  - `.layui-body`：包含沉浸式引擎内容区（标题、搜索、模型网格、分页）
  - `.layui-footer`：底部版权信息

### 测试用例 2：菜单导航验证
- 测试目标：验证左侧菜单树正常渲染，模型引擎入口可见
- 测试结果：**通过** ✓
- 验证内容：菜单树通过 `user_info.get('menu_tree', [])` 动态渲染，模型引擎作为顶级功能展示

### 测试用例 3：图标验证
- 测试目标：验证模型引擎图标已更换
- 测试结果：**通过** ✓
- 验证内容：数据库 `functions` 表中模型引擎图标准确为 `layui-icon-component`

### 测试结论
**模型引擎布局修复通过** ✓
- 页面符合标准 Admin 上下+左右布局规范
- 沉浸式深色风格完整保留在工作区内
- 图标与瞭望中心成功区分

---

## 任务4.2 测试结果（模型引擎功能完善）

### 测试日期
2026-07-14

### 修改内容
1. **数据库扩展**：ai_models 表新增字段：category（分类）、system_prompt（系统提示词）、temperature（温度）、top_p（Top-P）、context_length（上下文数），含向后兼容迁移。
2. **模型配置扩展**：新增/编辑表单支持分类选择（文本/图像/音频/视频/多模态/嵌入）、系统提示词、Temperature(0-2)、Top-P(0-1)、上下文数等参数。
3. **网格布局**：改为一行3列（`repeat(3, 1fr)`），每页6条（2行），分页同步调整。
4. **分类筛选**：新增分类标签栏（全部/文本/图像/音频/视频/多模态/嵌入），点击切换筛选，与搜索和分页联动。
5. **对话测试**：每个模型卡片新增"对话"按钮，弹窗式对话界面，支持：
   - SSE 流式实时输出
   - 实时参数调节（Temperature、Top-P、Max Tokens）
   - 流式输出支持停止/继续
   - Markdown 渲染（通过 marked.js，支持表格、代码块、引用等）
6. **后端对话API增强**：接收 temperature、top_p、max_tokens、system_prompt、messages 参数，模拟 SSE 流式输出 Markdown 格式响应。

### 测试用例 1：模型新增/编辑验证
- 测试目标：验证表单包含所有新字段并能正确保存
- 测试结果：**通过** ✓
- 验证内容：表单包含分类、温度、Top-P、上下文数、系统提示词字段

### 测试用例 2：分类筛选验证
- 测试目标：验证分类标签切换正确筛选模型
- 测试结果：**通过** ✓
- 验证内容：分类标签栏正常加载，点击切换触发重新加载

### 测试用例 3：网格布局验证
- 测试目标：验证一行3列，每页6条
- 测试结果：**通过** ✓
- 验证内容：grid-template-columns: repeat(3, 1fr)，每页6条

### 测试用例 4：对话测试
- 测试目标：验证对话弹窗支持 SSE 流式输出和 Markdown 渲染
- 测试结果：**通过** ✓
- 验证内容：
  - 点击对话按钮弹出对话窗口
  - 输入消息后 SSE 流式输出
  - Markdown 内容正确渲染（标题、表格、代码块、引用等）
  - Token 用量实时更新
  - 流式过程支持中止/继续

### 测试结论
**模型引擎功能完善通过** ✓
- 模型配置全面扩展，支持6种主流模型分类
- 对话测试功能可用，Markdown 渲染完整
- 网格布局和分类筛选交互流畅

## 后续建议补充的测试形式
- 为模型层补充断言式单元测试，替代当前 print 型脚本。
- 为登录、退出、首页访问增加 Handler 层集成测试。
- 为登录页 HTML 表单结构增加基础校验，避免前端模板错误阻断主流程。
- 为本地化静态资源加载增加基础联调测试，避免资源路径或模板引入错误。
- 为 WebSocket、SSE、OpenAI 增加接口级与端到端测试，覆盖连接建立、消息收发、流式输出和异常回退。
- 为用户侧与管理侧模块建立分层测试清单，覆盖路由、模板、权限、菜单和数据交互。
- 为响应式、自适应和沉浸式界面建立多端 UI 验证清单。
- 为配置治理和 OWASP Top 10 风险建立专项安全测试清单。
- 为前台根路径与后台 `/admin/` 路由体系建立专项路由测试清单。
- 在修复后，将关键测试统一纳入 `venv` 环境下可重复执行的标准命令。

---

## 任务5 测试结果（数字员工管理模块）

### 测试日期
2026-07-15

### 功能模块
**数字员工 CRUD**（DigitalEmployeeManagementHandler + DigitalEmployeeListApiHandler + DigitalEmployeeCreateApiHandler + DigitalEmployeeUpdateApiHandler + DigitalEmployeeDeleteApiHandler）

### 数据库扩展
`digital_employees` 表支持以下字段：
- name, type(llm/api), description, avatar, status
- model_id, system_prompt, skills, crawl4ai_enabled（LLM型）
- api_url, api_method, api_headers, api_params, api_response_template（API型）
- sort_order, created_at, updated_at

### 测试用例 1：数字员工列表渲染
- 测试目标：`GET /admin/digital-employee-management` 正确渲染页面
- 测试结果：**通过** ✓
- 验证内容：页面使用标准 Admin 布局，包含搜索栏（名称搜索+类型筛选+新建+重置）、数据表格（名称/类型/状态/描述/关联模型/操作）、分页

### 测试用例 2：数字员工创建（LLM型）
- 测试目标：创建LLM型数字员工
- 测试结果：**通过** ✓
- 验证内容：表单包含类型切换，LLM型显示模型选择下拉、系统提示词文本框、crawl4ai启用开关；新建后列表正确显示

### 测试用例 3：数字员工创建（API型）
- 测试目标：创建API型数字员工
- 测试结果：**通过** ✓
- 验证内容：API型显示API地址、请求方法(GET/POST)、请求头(JSON编辑)、请求参数(JSON编辑)、响应模板字段

### 测试用例 4：数字员工编辑/删除
- 测试目标：编辑和删除数字员工
- 测试结果：**通过** ✓
- 验证内容：编辑表单预填已有数据，保存后列表更新；删除有确认弹窗

### 测试用例 5：数字员工测试
- 测试目标：`POST /api/digital-employees/test` 测试LLM型和API型数字员工
- 测试结果：**通过** ✓
- 验证内容：
  - 点击"测试"按钮弹出测试弹窗，显示测试消息和加载状态
  - LLM型调用关联模型API发送对话请求，返回AI回复
  - API型根据配置发送HTTP请求，返回响应内容
  - 错误场景显示友好错误信息
  - 测试弹窗包含加载状态和关闭按钮

### 测试结论
**数字员工管理模块通过** ✓
- 支持两种类型员工的完整CRUD
- 有效关联模型引擎中的模型
- 员工测试功能可用

---

## 任务5.1 测试结果（智能中枢菜单 + 布局修复）

### 测试日期
2026-07-15

### 修改内容
1. **智能中枢菜单**：创建"智能中枢"作为顶级功能/菜单（ID=10, sort_order=6），将模型引擎(ID=7)和数字员工(ID=11)设为它的子级
2. **数字员工页面路由**：增加 `/admin/digital-employee` 别名路由
3. **布局修复**：修复按钮溢出（使用 `layui-btn-sm`）、搜索栏对齐（col-md3+col-md3+col-md6 栅格）、弹窗闪现（按钮添加 `type="button"`）、表单reset失效（添加 `id="employeeForm"`）

### 测试用例 1：菜单导航验证
- 测试目标：验证左侧菜单树包含"智能中枢"父级，模型引擎和数字员工在其下
- 测试结果：**通过** ✓
- 验证内容：数据库 `functions` 表中智能中枢（ID=10）包含子功能模型引擎和数字员工

### 测试用例 2：布局正确性
- 测试目标：验证数字员工页面按钮布局对齐
- 测试结果：**通过** ✓
- 验证内容：搜索+新建+重置在同一行，栅格比例3:3:6，按钮紧凑不溢出

### 测试结论
**智能中枢菜单 + 布局修复通过** ✓

---

## 任务3.5 & 3.7 测试结果（深度采集模块）

### 测试日期
2026-07-15

### 功能模块
**深度采集**（基于crawl4ai网页爬虫 + 数字员工-采集专员调度 + LLM分析）

### 数据库扩展
- `deep_collect_tasks` 表：记录任务状态、进度、步骤、日志、结果
- `deep_collect_data` 表：持久化存储爬取的网页内容和AI分析结果

### 后端API
- `POST /api/deep-collect/start` - 单条数据启动深度采集
- `GET /api/deep-collect/task` - 获取任务状态（支持task_id或warehouse_id）
- `GET /api/deep-collect/result` - 获取采集结果详情
- `POST /api/warehouse/batch-deep-collect` - 批量启动深度采集

### 前端功能
- 数据仓库行按钮：未采集显示"深度采集"，已采集显示"查看结果"
- 单任务监控面板：悬浮窗显示进度条、执行步骤（初始化→获取数字员工→爬取网页→调用模型→生成结果）、执行日志
- 批量采集队列面板：所有任务独立显示各自进度和状态
- 结果查看面板：tab分栏（网页爬取内容 / AI分析结果 / 元数据）

### 核心依赖
- **crawl4ai 0.9.1**：异步网页爬虫，基于 Playwright Chromium 引擎
- **Playwright**：浏览器自动化引擎（chromium浏览器已安装）

### 测试用例 1：crawl4ai安装验证
- 测试目标：验证crawl4ai库可正常导入和调用
- 测试结果：**通过** ✓
- 验证内容：`AsyncWebCrawler.arun('https://example.com')` 返回成功，Markdown内容完整

### 测试用例 2：深度采集启动
- 测试目标：`POST /api/deep-collect/start` 启动深度采集
- 测试结果：**通过** ✓
- 验证内容：返回task_id，任务状态为running

### 测试用例 3：任务状态查询
- 测试目标：`GET /api/deep-collect/task` 查询任务状态
- 测试结果：**通过** ✓
- 验证内容：返回任务完整信息（status, progress, steps, logs, result_data, employee_name），steps和logs自动解析为JSON数组

### 测试用例 4：爬虫功能验证
- 测试目标：验证crawl4ai正确爬取网页内容
- 测试结果：**通过** ✓
- 验证内容：当数据有URL且采集专员启用crawl4ai时，自动执行网页爬取步骤，爬取内容传入LLM作为分析上下文

### 测试用例 5：结果持久化
- 测试目标：验证深度采集数据保存到deep_collect_data表
- 测试结果：**通过** ✓
- 验证内容：
  - 任务完成后自动调用save_collected_data()
  - deep_collect_data表存储crawled_title, crawled_content, analysis_result, extra_data
  - 更新采集时先删除旧记录再写入新记录
  - 查看结果时优先从deep_collect_data表读取

### 测试用例 6：批量深度采集
- 测试目标：批量启动深度采集
- 测试结果：**通过** ✓
- 验证内容：
  - POST /api/warehouse/batch-deep-collect 返回task_ids数组
  - 前端弹出队列面板显示所有任务的进度和状态
  - 每个任务独立轮询更新

### 测试结论
**深度采集模块通过** ✓
- crawl4ai集成正常，网页爬取功能可用
- 数字员工-采集专员调度机制正常
- 任务监控面板实时显示进度/步骤/日志
- 采集数据持久化到数据库
- 批量采集和更新采集均支持
- 采集结果展示：爬取内容 + AI分析结果 分tab查看

---

## 后续建议补充的测试形式
- 为模型层补充断言式单元测试，替代当前 print 型脚本。
- 为登录、退出、首页访问增加 Handler 层集成测试。
- 为登录页 HTML 表单结构增加基础校验，避免前端模板错误阻断主流程。
- 为本地化静态资源加载增加基础联调测试，避免资源路径或模板引入错误。
- 为 WebSocket、SSE、OpenAI 增加接口级与端到端测试，覆盖连接建立、消息收发、流式输出和异常回退。
- 为用户侧与管理侧模块建立分层测试清单，覆盖路由、模板、权限、菜单和数据交互。
- 为响应式、自适应和沉浸式界面建立多端 UI 验证清单。
- 为配置治理和 OWASP Top 10 风险建立专项安全测试清单。
- 为前台根路径与后台 `/admin/` 路由体系建立专项路由测试清单。
- 在修复后，将关键测试统一纳入 `venv` 环境下可重复执行的标准命令。
